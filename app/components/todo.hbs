<li class="{{if @todo.completed 'completed'}}">
  <div class="view">
    <input
      class="toggle"
      type="checkbox"
      aria-label="Toggle the completion of this todo"
      checked={{@todo.completed}}
      {{ on 'change' (fn this.todoData.toggleCompletion @todo) }}
    >
    <label>{{@todo.title}}</label>
    <button
      type="button"
      class="destroy"
      title="Remove this todo"
      {{ on 'click' (fn this.todoData.remove @todo) }}
    ></button>
  </div>

  <input autofocus class="edit" value="Todo Title">
</li>